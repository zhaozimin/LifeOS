# web/
> L2 | 父级: ../CLAUDE.md

成员清单
index.html: history 深链 fallback 的 LifeOS SPA 入口镜像。
dashboard/: Vite 预构建入口、带哈希 JS/CSS 资产及 PWA 图标；仅由 web-dashboard 构建生成。

法则：此目录只接受可复现发布产物；不包含 Token、账本、附件、源码或 Service Worker。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
